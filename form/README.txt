a basic form to select genre and query the db. I got sick of using
React to do very simple things, so just serve this with
$ python -m SimpleHTTPServer
or whatever.

Note you may need to change the base_url value in movies.js depending
on where you run the react server.
